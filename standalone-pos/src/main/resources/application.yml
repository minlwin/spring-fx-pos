spring:
  datasource:
    url: jdbc:hsqldb:file:database/pos
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true